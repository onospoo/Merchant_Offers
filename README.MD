# Datasourse loader service

Для запуска использовать команду `docker-compose up`.

Для запуска апи использовать swagger по адресу `localhost:8080/swagger-ui.html`.

/load - загрузка данных из источников.